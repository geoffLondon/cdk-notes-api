type Note {
    id: ID!
    name: String!
    completed: Boolean!
    params: AWSJSON!
}

input NoteInput {
    id: ID!
    name: String!
    completed: Boolean!
    params: AWSJSON!
}

type Query {
    listNotes: [Note]
}

type Mutation {
    createNote(params: NoteInput!): Note
}
