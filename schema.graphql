schema {
    query: Query
    mutation: Mutation
}

type Note {
    id: String!
    name: String!
    completed: Boolean
}

type Query {
    listNotes: [Note]
}

type Mutation {
    createNote(id: String!, name: String!, completed: Boolean): Note!
}
