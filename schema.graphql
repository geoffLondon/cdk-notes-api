schema {
    query: Query
    mutation: Mutation
}

type Note {
    name: String!
    completed: Boolean
}

type Query {
    listNotes: [Note]
}

type Mutation {
    createNote(name: String!, completed: Boolean): Boolean
}
